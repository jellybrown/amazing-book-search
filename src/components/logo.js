import React from "react";

const Logo = () => {
  return (
    <div>
      <span style={{ fontSize: "2rem", fontWeight: "bold" }}>Book.</span>
      <span style={{ fontSize: "1rem" }}>search</span>
    </div>
  );
};

export default Logo;
